namespace SharpEnd.Packet
{
    //create an enum with all the HTTP Methods
    public enum RequestMethod
    {
        GET,
        POST,
        PUT,
        DELETE,
        HEAD,
        OPTIONS,
        TRACE,
        CONNECT
    }
}
